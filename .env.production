# Production build config (read by Vite during `npm run build`).
# The API runs on its own subdomain (cPanel "Setup Node.js App"); the React app
# is served from accounting.astrochakra.co, so this must be the full absolute URL.
VITE_API_BASE=https://api.astrochakra.co/api
